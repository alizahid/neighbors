import { type NextPageWithLayout } from 'next'

const Page: NextPageWithLayout = () => (
  <div className="flex min-h-screen items-center justify-center">
    <h1 className="text-4xl font-bold text-primary-11">Neighbors</h1>
  </div>
)

export default Page
